Table layout for exercises and workouts and stuff
(Every table will also have metadata)

I think this is the minimum needed for the basic features I envision.

exercise_types
    id
    user_id --> 0 if default
    is_default
    name
    image
    ppl_type
    muscle_group

setgroup_logs
    id
    exercise_log_id
    sets
    reps
    weight

exercise_logs
    id
    workout_log_id
    exercise_type_id
    notes

workout_logs
    id
    user_id
    date
    name
    notes

setgroup_templates
    id
    exercise_template_id
    sets
    reps

exercise_templates
    id
    workout_template_id
    exercise_type_id

workout_templates
    id
    user_id
    name